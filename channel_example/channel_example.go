/*This sample is used to test the channel package*/

package main

import (
	"fmt"

	"github.com/kesavand/golang/concurrency/channel"
)

func main() {
	fmt.Println("Starting the producer and the consumer")
//	ch := make(chan int)
//	channel.Producer(ch)
}
